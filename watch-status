#!/bin/bash
# Symlink to the orchestrator's watch-status script
# This file should be a symlink: ln -sf .claude/orchestrator/watch-status watch-status

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/.claude/orchestrator/watch-status" "$@"
